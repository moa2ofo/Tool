name: CI

on:
  push:
    branches: ["**"]

permissions:
  contents: write

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  format_and_unit_tests:
    runs-on: ubuntu-latest
    env:
      TZ: Europe/Rome
    if: ${{ github.ref != 'refs/heads/main' && github.ref != 'refs/heads/develop' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Pull early to avoid "unstaged changes" rebase errors
      - name: Sync branch with remote (push only)
        if: github.event_name == 'push'
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git pull --rebase

      # -----------------------------
      # clang-format (always runs)
      # -----------------------------
      - name: Install clang-format
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format

      # -----------------------------
      # Get unitTestLauncher repo
      # -----------------------------
      - name: Checkout unitTestLauncher repo
        uses: actions/checkout@v4
        with:
          repository: moa2ofo/unitTestLauncher
          path: unitTestLauncher
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"


      - name: Debug clang-format config presence
        run: |
          clang-format --version
          echo "Repo root:"
          ls -la
          echo "unitTestLauncher content:"
          ls -la unitTestLauncher || true
          echo "Check .clang-format:"
          ls -la unitTestLauncher/.clang-format || true

      - name: Run clang-format on all .c and .h files
        id: clang_format
        run: |
          find . -type f \( -name "*.c" -o -name "*.h" \) \
            -not -path "./build/*" \
            -not -path "./.git/*" \
            -exec clang-format --style=file --assume-filename=unitTestLauncher/.clang-format -i {} \;

      - name: Verify Docker is available
        id: docker_info
        continue-on-error: true
        run: sudo docker info

      # -----------------------------
      # swCmpBuildCheck (always runs)
      # -----------------------------
      - name: Run swCmpBuildCheck (launcher repo)
        id: sw_cmp_build_check
        continue-on-error: true
        run: |
          sudo -E python3 unitTestLauncher/swCmpBuildCheck.py

      # -----------------------------
      # Unit tests (always runs)
      # -----------------------------
      - name: Fix permissions for unitTestLauncher workspace
        id: fix_permissions
        continue-on-error: true
        run: |
          sudo mkdir -p unitTestLauncher/utExecutionAndResults/utUnderTest/build
          sudo chmod -R 777 unitTestLauncher/utExecutionAndResults

      - name: Run unit tests (launcher repo)
        id: unit_tests
        continue-on-error: true
        run: |
          script -q -e -c "python3 unitTestLauncher/unitTestsLauncher.py all" /dev/null

      - name: Replace utResults with launcher results (robust)
        id: replace_ut_results
        if: always()
        continue-on-error: true
        run: |
          sudo apt-get update
          sudo apt-get install -y rsync

          mkdir -p utResults

          if [ -d "unitTestLauncher/utExecutionAndResults/utResults" ]; then
            rsync -a --delete unitTestLauncher/utExecutionAndResults/utResults/ utResults/
          else
            echo "No launcher utResults directory found; skipping rsync."
          fi
      - name: Delete unitTestLauncher folder
        if: always()
        run: |
          sudo rm -rf unitTestLauncher
          echo "unitTestLauncher folder removed."


      # -----------------------------
      # Single commit at the end (success OR failure)
      # Writes ci_status/last_status with full stage outcomes.
      # -----------------------------
      - name: Commit and push changes (format + buildCheck + utResults)
        if: github.event_name == 'push' && always()
        env:
          FORMAT_OUTCOME: ${{ steps.clang_format.outcome }}
          DOCKER_OUTCOME: ${{ steps.docker_info.outcome }}
          BUILD_OUTCOME:  ${{ steps.sw_cmp_build_check.outcome }}
          UT_OUTCOME:     ${{ steps.unit_tests.outcome }}
          RSYNC_OUTCOME:  ${{ steps.replace_ut_results.outcome }}
          RUN_URL:        ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"

          map_outcome() {
            case "$1" in
              success) echo "performed" ;;
              skipped) echo "skipped" ;;
              *)       echo "failed" ;;
            esac
          }

          FORMAT_STATUS="$(map_outcome "$FORMAT_OUTCOME")"
          DOCKER_STATUS="$(map_outcome "$DOCKER_OUTCOME")"
          BUILD_STATUS="$(map_outcome "$BUILD_OUTCOME")"
          UT_STATUS="$(map_outcome "$UT_OUTCOME")"
          RSYNC_STATUS="$(map_outcome "$RSYNC_OUTCOME")"

          FAILED=0
          for s in "$FORMAT_STATUS" "$DOCKER_STATUS" "$BUILD_STATUS" "$UT_STATUS" "$RSYNC_STATUS"; do
            if [ "$s" = "failed" ]; then
              FAILED=1
              break
            fi
          done

          mkdir -p ci_status

          if [ "$FAILED" -eq 1 ]; then
            STATUS_LINE="CI FAILED"
          else
            STATUS_LINE="CI PASSED"
          fi

          {
            echo "${STATUS_LINE}"
            echo "Run: ${RUN_URL}"
            echo "clang-format: ${FORMAT_STATUS}"
            echo "docker info: ${DOCKER_STATUS}"
            echo "swCmpBuildCheck: ${BUILD_STATUS}"
            echo "unit tests: ${UT_STATUS}"
            echo "rsync utResults: ${RSYNC_STATUS}"
            echo "Timestamp (UTC): $(date -u +"%Y-%m-%dT%H:%M:%SZ")"
          } > ci_status/last_status

          git add -A

          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            if [ "$FAILED" -eq 1 ]; then
              git commit -m "CI failed: fmt=${FORMAT_STATUS}, docker=${DOCKER_STATUS}, build=${BUILD_STATUS}, ut=${UT_STATUS}, rsync=${RSYNC_STATUS} [skip ci]"
            else
              git commit -m "unit test performed [skip ci]"
            fi
            git push
          fi

          if [ "$FAILED" -eq 1 ]; then
            echo "One or more steps failed."
            exit 1
          fi

